# Clean-Transform-CSV
A mini-project using Python and Pandas to clean messy CSV data. Handles missing values, duplicates, inconsistent formats, and invalid entries, transforming raw data into a clean, structured format ready for analysis or further processing.
